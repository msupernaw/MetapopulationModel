#ifndef SUBPOPULATION_HPP
#define	SUBPOPULATION_HPP

/**
 *Used to define the location of a subpopulation.
 */
template<class T>
class Polygon {
};

template<class T>
class Movement {
};

template<class T>
class Structure {
};

template<class T>
class Subpopulation {
    
    Movement<T>* movement_m;
    Structure<T>* structure_m;
    Polygon<T>* polygon_m;
    
    public:
    

};
